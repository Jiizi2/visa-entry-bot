# Data Layout

Supported input layouts:

`data/<GROUP_ID>/passports/`

`<GROUP_ID>/passports/`

`<GROUP_ID>/passport/`

You can also pass a folder path directly, for example:

```powershell
cd python-ocr
python main.py example/group
```
